# remap prefix from 'C-b' to 'C-a'
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

set -g default-terminal "xterm-256color"        # allow prettry colors
set -g mouse on                                 # mouse support

# Color - see themepack link at the end
source-file "${HOME}/.local/share/dotfiles/tmux-themepack/powerline/block/blue.tmuxtheme"

setw -g mode-keys vi                                        # Make it vimi
unbind-key -T copy-mode-vi v
bind-key -T copy-mode-vi 'v' send -X begin-selection        # Begin selection in copy mode.
bind-key -T copy-mode-vi 'C-v' send -X rectangle-toggle     # Begin selection in copy mode.
bind-key -T copy-mode-vi 'y' send -X copy-selection         # Yank selection in copy mode.

bind r source-file ~/.tmux.conf                             # reload config file for debugging

# ------------------------vim-plugin-integration-----------------------#
# --------Smart pane switching with awareness of Vim splits------------#
# --------See: https://github.com/christoomey/vim-tmux-navigato--------#
# ---------------------------------------------------------------------#
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
bind-key -n C-h if-shell "$is_vim" "send-keys C-h"  "select-pane -L"
bind-key -n C-j if-shell "$is_vim" "send-keys C-j"  "select-pane -D"
bind-key -n C-k if-shell "$is_vim" "send-keys C-k"  "select-pane -U"
bind-key -n C-l if-shell "$is_vim" "send-keys C-l"  "select-pane -R"
bind-key -n C-\ if-shell "$is_vim" "send-keys C-\\" "select-pane -l"
bind-key -T copy-mode-vi C-h select-pane -L
bind-key -T copy-mode-vi C-j select-pane -D
bind-key -T copy-mode-vi C-k select-pane -U
bind-key -T copy-mode-vi C-l select-pane -R
bind-key -T copy-mode-vi C-\ select-pane -l
# ---------------------------------------------------------------------#
# ---------------------------------------------------------------------#

# https://www.hamvocke.com/blog/a-guide-to-customizing-your-tmux-conf/
# https://superuser.com/questions/1294618/how-can-i-navigate-terminal-output-like-a-text-file-with-vim-keybinding
# https://sanctum.geek.nz/arabesque/vi-mode-in-tmux/
# https://github.com/jimeh/tmux-themepack
